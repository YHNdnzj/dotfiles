include ~/.config/sway/config.common

### Output configuration
output eDP-1 res 2560x1440 scale 1.5 adaptive_sync on

### Input configuration
input 14:13330:BT5.0_Mouse {
    events disabled
}

### Idle configuration
exec swayidle -w \
        timeout 900 'swaylock -f' \
        timeout 930 'swaymsg "output * power off"' resume 'swaymsg "output * power on"' \
        before-sleep 'playerctl pause' before-sleep 'swaylock -f'

# vim: set ft=swayconfig ts=4 sw=4 et:
