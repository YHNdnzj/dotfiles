#!/bin/bash -e

if ! pidof -q neomutt; then
    mkdir -p "$HOME/Downloads/mutt"
    cd "$HOME/Downloads/mutt"
    export http_proxy=http://127.0.0.1:8118
    export https_proxy=$http_proxy
    exec neomutt
else
    echo "Mutt is already running, exiting..." >&2
    exit 1
fi
